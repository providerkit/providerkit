node_modules
dist
*.tsbuildinfo
.DS_Store

# Copied in at publish time from the repo root — one source of truth.
core/README.md
core/LICENSE

# Astro's generated types, and the TypeDoc reference — both rebuilt by
# `bun run build:site`, so committing them would just add noise to every diff.
site/.astro/
site/src/content/docs/reference/

# Internal: the product/brand brief and the migration roadmap both name private
# codebases. This repo is public — they stay on disk and out of it.
PRODUCT.md
ROADMAP.local.md
